{
  "variables": {
    "image_id": "{{env `ANKA_IMAGE_ID`}}"
  },
  "provisioners": [
    {
      "type": "shell",
      "inline": [
        "pwd"
      ]
    }
  ],
  "builders": [{
    "type": "veertu-anka",
    "installer_app": "/Applications/Install macOS Sierra.app/",
    "disk_size": "25G",
    "image_id": "{{user `image_id`}}"
  }]
}
